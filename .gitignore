# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store

# Vendored TanStack Query docs and query-core tests. Not committed — fetch them
# at the pinned revision with ./scripts/vendor-reference.sh, which is where that
# revision is recorded. Required before gate-2 (test) work; see CONTRIBUTING.md.
.reference/

# The roadmap is working material: gate-1 specs, open questions, parity tables
# and the reasoning behind decisions. Useful while building, noise for someone
# consuming the library.
docs/roadmap/

# Maven Central staging output; regenerate with ./gradlew centralBundle
build/central*/

# Go-to-market planning. Working material, not published.
gtm.md
.playwright-mcp/
video-script.md
