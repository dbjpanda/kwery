# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties

# IDE
.idea/
*.iml
.kotlin/

# OS
.DS_Store

# Vendored TanStack Query docs and query-core tests. Not committed — fetch them
# at the pinned revision with ./scripts/vendor-reference.sh, which is where that
# revision is recorded. Required before gate-2 (test) work; see CONTRIBUTING.md.
.reference/
